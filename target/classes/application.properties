spring.application.name=VehicleParkingSystem

# Database Configuration (H2) - DISABLED
# spring.datasource.url=jdbc:h2:mem:parking_system
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.h2.console.enabled=true

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/parking_system?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always

# Server Port
server.port=8083
